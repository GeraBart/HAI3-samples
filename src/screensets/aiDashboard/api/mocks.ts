/**
 * aiDashboard API Mocks
 * Mock data for development and testing
 */

import type { MockMap } from '@hai3/uicore';

/**
 * Mock map for aiDashboard API service
 * Add your mock endpoints here
 *
 * Example:
 * 'GET /items': () => mockItems,
 * 'POST /items': (data) => ({ id: Date.now(), ...data }),
 */
export const aiDashboardMockMap = {
  // Add your mocks here
} satisfies MockMap;
